name: Branch Guard

on:
  pull_request:
    branches: [main]

jobs:
  check-source-branch:
    name: dev → main 브랜치 검증
    runs-on: ubuntu-latest
    steps:
      - name: Verify PR source is dev
        env:
          HEAD_REF: ${{ github.head_ref }}
        run: |
          if [ "$HEAD_REF" != "dev" ]; then
            echo "❌ main 브랜치는 dev 브랜치에서의 PR만 허용합니다."
            echo "현재 소스 브랜치: $HEAD_REF"
            exit 1
          fi
          echo "✅ dev → main PR 확인 완료"
